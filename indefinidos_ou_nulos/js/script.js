// undefined
// A variable that is declared but not assigned(recebeu) any value yet.
//let a;
//console.log(a);

// null 
// A value that intentionally(intencionalmente) represents "no value" or "null".
let a = null;
console.log(a);

if (a === null) {
    console.log('null is printed');
}

// Real- world Use Cases
//Checking if API response is empty.

//Checking if a user input is missing.

//handling optional properties in objects.