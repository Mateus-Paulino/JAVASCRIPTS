//condition ? expressionIfTrue : expressionIfFalse

//? means if true
//: means else

//const age = 10;

//const canVote = (age >= 18) ? "Yes, you can vote" : "No, you cant";

//console.log(canVote);

const random = Math.random(); // the random value is between 0 and 1
console.log(random);
const result = (random>0.5) ? "Big Number" : "Small Number";

console.log(result);